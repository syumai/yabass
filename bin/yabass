#!/usr/bin/env ruby

require_relative '../lib/yabass'

yabass = Yabass::Yabass.new(Dir.pwd)
case ARGV.first
when /^g(enerate)?$/
  yabass.generate
  yabass.routes
when /^r(outes)?$/
  yabass.routes
when /^s(erver)?$/
  yabass.server
end

