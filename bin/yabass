#!/usr/bin/env ruby

require_relative '../lib/yabass'

if ARGV.first === 'generate'
  yabass = Yabass::Yabass.new(Dir.pwd)
  yabass.generate
  yabass.routes
elsif ARGV.first === 'routes'
  yabass = Yabass::Yabass.new(Dir.pwd)
  yabass.routes
end

